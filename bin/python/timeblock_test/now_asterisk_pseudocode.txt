NOW=now up to minute

Find 'now' spot in output table.

Insert '*' in now

# because it has 15 chars per cell
# 60/15=4, 30/15=2, 15/15=1
if detail = hour
  each * is 4 minutes
if detail = half
  each * is 2 minutes
if detail = quarter
  each * is 1 minutes
