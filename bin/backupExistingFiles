#!/usr/bin/env bash

cd $HOME/home
git ls-tree --name-only master >> trackedFiles.txt
cd $HOME

EXTENSION=pre-deploy

for FILE in $(cat $HOME/home/trackedFiles.txt); do
	mv "$HOME/$FILE" "$HOME/$FILE.$EXTENSION"
	echo "Renaming $FILE into $FILE.$EXTENSION"
done
